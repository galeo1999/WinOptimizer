<UserControl x:Class="WinOptimizer.Views.StorageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#111827">

    <ScrollViewer VerticalScrollBarVisibility="Auto">

        <Grid Margin="30">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->

            <StackPanel Grid.Row="0">

                <TextBlock Text="Storage"
                           Foreground="White"
                           FontSize="28"
                           FontWeight="Bold"/>

                <TextBlock Text="Analyze temporary files, downloads and other storage locations."
                           Foreground="#9CA3AF"
                           FontSize="14"
                           Margin="0,5,0,0"/>

            </StackPanel>


            <!-- Actions -->

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal">

                <Button x:Name="AnalyzeStorageButton"
                        Content="Analyze Storage"
                        Padding="16,9"
                        Click="AnalyzeStorageButton_Click"/>

                <TextBlock x:Name="StorageStatusText"
                           Text="Ready"
                           Foreground="#9CA3AF"
                           FontSize="13"
                           VerticalAlignment="Center"
                           Margin="15,0,0,0"/>

            </StackPanel>


            <!-- Summary -->

            <Border Grid.Row="4"
                    Background="#1F2937"
                    CornerRadius="12"
                    Padding="20">

                <StackPanel>

                    <TextBlock Text="Detected Storage"
                               Foreground="#9CA3AF"
                               FontSize="13"/>

                    <TextBlock x:Name="TotalStorageText"
                               Text="Not analyzed"
                               Foreground="White"
                               FontSize="26"
                               FontWeight="SemiBold"
                               Margin="0,6,0,0"/>

                    <TextBlock x:Name="StorageSummaryText"
                               Text="Run an analysis to inspect storage locations."
                               Foreground="#9CA3AF"
                               FontSize="12"
                               Margin="0,5,0,0"/>
                    <TextBlock x:Name="CleanupPotentialText"
                               Text=""
                               Foreground="#22C55E"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="0,8,0,0"/>

                </StackPanel>

            </Border>


            <!-- Results -->

            <StackPanel x:Name="StorageResultsPanel"
                        Grid.Row="6"/>

        </Grid>

    </ScrollViewer>

</UserControl>